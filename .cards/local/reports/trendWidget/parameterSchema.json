{
  "$id": "trendWidgetReportSchema",
  "additionalProperties": false,
  "description": "A line chart showing how one value moves across a series of periods",
  "properties": {
    "name": {
      "description": "The name of the report",
      "type": "string"
    },
    "title": {
      "description": "Title text on top of the chart",
      "type": "string"
    },
    "legend": {
      "description": "Description text shown below the chart (optional)",
      "type": "string"
    },
    "colour": {
      "description": "Colour of the line (optional, defaults to blue)",
      "type": "string",
      "enum": ["blue", "green", "yellow", "red", "orange", "purple"]
    },
    "points": {
      "description": "The value at each period, oldest first. Only the last value is labelled; period names that do not fit are dropped rather than overlapped, so keep them short.",
      "type": "array",
      "minItems": 2,
      "items": {
        "additionalProperties": false,
        "type": "object",
        "properties": {
          "label": {
            "description": "Name of this period, shown on the horizontal axis",
            "type": "string"
          },
          "value": {
            "description": "Value at this period",
            "type": "number"
          }
        },
        "required": ["label", "value"]
      }
    }
  },
  "required": ["name", "title", "points"],
  "title": "Report",
  "type": "object"
}
